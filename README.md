# reTODOs

这是跟随Raywenderlich.com课程所做的Git练习reop。

//MSCompiler Driver By QStudio - End
//查看预编译参数使用方法请点击 菜单->构建 [B]->编译帮助 [H]

#include <iostream>
using namespace std;
int main(int argc, char * argv[])
{
	int num = 0;
	cout << "你好,QStudio ! 请输入一个数字!" << endl;
	cin >> num;
	cout << num << endl;
	return 0;
}
